# Dependencies
node_modules
frontend/node_modules

# Environment & IDE
.env
.env.local
.DS_Store
.vscode
.idea
*.log

# Uploads (keep directory structure)
api/uploads/*
!api/uploads/.gitkeep

# Real DB credentials live only on the server, never in git
api/env.php
